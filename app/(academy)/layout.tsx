// Minimal layout for the lesson player — no public header/footer
// so learners get a distraction-free, full-screen experience.
export default function AcademyLessonLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
