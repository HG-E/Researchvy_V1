import { NextResponse } from "next/server";

// TODO: Implement analytics route (Phase 1C-1F)
export async function GET() {
  return NextResponse.json({ message: "Route stub — implementation in upcoming phase" }, { status: 501 });
}
